import React from 'react';

const Title = (props) => { 
    return(
        <div className='title'>
            <h4 className='title-text'>{props.title}</h4>
        </div>
    );
  }
  export default Title;